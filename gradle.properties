# Project version (single source of truth)
version=1.1.0

# Gradle settings
org.gradle.jvmargs=-Xmx2g -XX:+HeapDumpOnOutOfMemoryError
org.gradle.parallel=true
org.gradle.caching=true

# Maven Central credentials (set in ~/.gradle/gradle.properties or via env vars)
# mavenCentralUsername=your-central-portal-token-username
# mavenCentralPassword=your-central-portal-token-password

# GPG Signing (set in ~/.gradle/gradle.properties or via env vars)
# signingInMemoryKey=your-gpg-private-key-armored
# signingInMemoryKeyPassword=your-gpg-passphrase
